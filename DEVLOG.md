# DEVLOG — fellmise_site

История работ по сайту `fellmise.com`. Новые записи — сверху, время локальное.
Правила ведения — общие для проектов (см. глобальный `CLAUDE.md`).

---

## 2026-07-26 21:05 — Итерация 4: скролл-путешествие по 5 биомам, фиксы деревни, глубокий дефринж
- **Что:** страница превращена в путешествие: деревня → лес → шахта → мир духов → дом.
  Каждый биом — полноэкранная секция со своим грунтом и небом, между ними проёмы
  (дверь дома, вход шахты, портал, дверь склепа) с зумом на scale + статичной
  radial-маске и проявлением следующего биома из темноты. Параллакс 2–3 слоя на
  IntersectionObserver. Ниже 760px и при prefers-reduced-motion проёмы убираются
  целиком — биомы становятся обычными секциями с тем же контентом. Деревня
  поправлена: левый дом вынесен на обочину, дорога получила рваную кромку, колеи,
  крапинки и траву, наезжающую на край; булыжник убран с проезжей части, указатель
  переставлен на кромку у края кадра. У изометрических спрайтов тень — скошенный
  эллипс под основанием. Сгенерировано 8 биом-пропсов.
- **Тип:** feat
- **Проверка:** Playwright 360/390/430/760/1280 × EN/RU со сквозным скроллом — 10/10 ok:
  горизонтального скролла нет, все 5 биомов на месте, 4 гейта, 9 карточек, 13 ресурсов,
  53 картинки без битых, ни один спрайт не «летает» (зазор арт↔бокс ≤4px), статик-режим
  включается ровно ниже 760px ✓ · вес первого экрана **382 KB** (цель <600) ✓ ·
  весь сайт **1.26 MB** (цель <2.5) ✓ · **Lighthouse mobile perf 83** (цель ≥80),
  TBT 0 ms, CLS 0.034 — резать параллакс не потребовалось ✓ · дефринж: кант 2px
  относительно внутренности по паку **+53.3 → −8.4** ✓ · live https://fellmise.com
  отдаёт `styles.css?v=4`, 5 биомов, 4 гейта ✓ · runClient n/a
- **Commit:** 814749f, HEAD

**Impact trace**

Создано (`fellmise_site/`):
- `tools/biomes.py` — описание 5 биомов (спрайты, глубины, iso-флаг, проёмы, карточки)
- `tools/gen_batch4.py`, `tools/finish_batch4.py`
- `assets/biome_pine_a.*`, `biome_pine_b.*`, `biome_stump.*`, `biome_orevein.*`,
  `biome_crystals.*`, `biome_brazier.*`, `biome_deadtree.*`, `biome_portal.*` (WebP+PNG, 560px)

Изменено (`fellmise_site/`):
- `styles.css` — блок `.hero*` заменён на `.biome*`: грунты/небо по биомам, дорога с
  clip-path и колеями, гейты (sticky-сцена, veil с radial-маской), тени `is-iso`,
  размеры спрайтов в % от сцены, статик-фолбэк `.is-static`, плашка времени возвращена
- `main.js` — журнал скролла: прогресс гейтов, шторки биомов, параллакс, промоушен
  lazy→eager на секцию вперёд; ранний выход в статик-режиме
- `tools/build_site.py` — генерация биомов вместо одиночного hero + секции фич
- `tools/grade_sprites.py` — дефринж на глубину 3px с чистым источником изнутри
- `tools/export_web.py` — ширина `biome_* 560px`
- `index.html`, `ru/index.html`, `assets/` (весь пак переэкспортирован после нового дефринжа)
- `out/site_assets/` — 32 новых кадра, 8 финалов, `report.json`

Изменено (`fellmise/`):
- `tracker/marketing/marketing.md` — «сайт итерация 4 (биомы)»

⚠️ Расхождение по housing из итерации 3 остаётся открытым (GDD этим батчем не трогался).

---

## 2026-07-26 20:21 — Итерация 3: анти-ИИ-проход, hero v2, Vollkorn, Vendetta, RU заново
- **Что:** дефринж и единая цветокоррекция всего пака (36 спрайтов), hero-сцена
  пересобрана (телега и забор убраны, добавлены 4 дорожных пропа, разные масштабы,
  кроны деревьев перекрывают дома, мягкая контактная тень под каждым спрайтом),
  шрифт текста Nunito → Vollkorn, карточка Nemesis → **Vendetta** с переименованием
  спрайта и всех ссылок, полоса ресурсов подрезана до 13, весь русский текст написан
  заново (не перевод), английский вычищен от шаблонности. Перегенерены `feat_skills`
  (лук больше не вплетается — меч и секира на крюках) и `feat_craft` (молот лежит на
  наковальне + светящаяся заготовка).
- **Тип:** feat
- **Проверка:** Playwright 360/390/430/760/1280 × EN/RU — 10/10 ok, горизонтального
  скролла нет ✓ · шрифты по факту: лого/слоган/заголовки Podkova, текст и кнопки
  Vollkorn, плашка и тултипы PT Mono ✓ · дефринж: дельта «кольцо − внутренность»
  по паку +64.7 → +24.0 ✓ · насыщенность σ 0.137 → 0.123 ✓ · grep «nemesis» по репо
  — 0 совпадений, на живой странице 0 ✓ · вес страницы 1.09 MB при цели <1.5 MB ✓ ·
  live https://fellmise.com отдаёт `styles.css?v=3`, 9 карточек, 13 ресурсов ✓ ·
  runClient n/a
- **Commit:** 0abce01, 3e01189

**Impact trace**

Создано (`fellmise_site/`):
- `tools/grade_sprites.py` — дефринж + общий грейд, пишет `out/site_assets/final_web/`
- `tools/gen_batch3.py`, `tools/finish_batch3.py`
- `assets/prop_signpost.*`, `prop_lantern.*`, `prop_crates.*`, `prop_stones.*` (WebP+PNG, 360px)

Изменено (`fellmise_site/`):
- `styles.css` — Vollkorn вместо Nunito, hero v2 (позиции, z-порядок, размеры по ВЫСОТЕ, тени `::after`), иконки полосы 104px, мобильный горизонт поднят
- `tools/build_site.py` — шрифты, список hero-спрайтов, 13 ресурсов, карточка Vendetta, весь копирайт EN/RU, `?v=3`
- `tools/export_web.py` — читает `final_web/`, добавлена ширина `prop_* 360px`
- `tools/filter_site_pack.py`, `tools/site_tasks.py` — id `feat_vendetta`
- `index.html`, `ru/index.html` — перегенерированы
- `assets/` — все спрайты переэкспортированы после дефринжа и грейда; `feat_nemesis.*` → `feat_vendetta.*`; `feat_skills`, `feat_craft` заменены перегенерацией
- `out/site_assets/final/` — `feat_skills`, `feat_craft` (перегенерация), 4 новых пропа, спрайт мести переименован; `report.json` обновлён

⚠️ **Расхождение, требует решения (GDD этим батчем не трогался):** новый текст карточки
«Дом» прямо называет захват свободной земли и постройку слоями. Это соответствует
разделу GDD «Строительство (решено)», но противоречит строке-ограничению, добавленной
в блок «⚠️ ОТКРЫТО — Жильё игрока (housing)» в прошлом батче («формулировки сайта не
расширять»). Либо ограничение сужается до реально открытых пунктов (б/г/д), либо текст
сайта откатывается к «база + хранение».

---

## 2026-07-26 19:00 — Итерация 2: мобильная вёрстка, новая типографика, карточка «Дом», +7 спрайтов
- **Что:** пофикшена вёрстка <760px (слоган-доска и CTA вынесены из hero в отдельную
  травяную полосу под сценой, слоган и дескриптор объединены в одну доску, бургер-меню).
  Типографика переведена на Podkova (дисплей) + Nunito (текст/кнопки) + PT Mono (служебное);
  Press Start 2P и Alegreya убраны полностью, включая логотип. Добавлена sticky-шапка с
  якорями Features / World / Devlog (плейсхолдер-поповер) и EN/RU. Девятая карточка «Дом»
  на переиспользованном `hero_house_b` (алиас `feat_home`, 640px). Арт: `res_wood`
  перегенерён (v1 давал пень — теперь сосна со штабелем брёвен), добавлено 6 новых
  ресурсных спрайтов, полоса ресурсов пересобрана по ролям (добыча / инструменты /
  оружие / магия), 16 позиций с тултипами EN/RU.
- **Тип:** feat
- **Проверка:** headless-прогон (Playwright/Chromium) на 360 / 390 / 430 / 760 / 1280 px
  в обоих языках — 10/10 ok ✓ · пойман и починен реальный баг: горизонтальный скролл
  страницы на всех мобильных ширинах (grid-элемент с `min-width: auto` + nowrap-полоса
  ресурсов раздували трек до ~1260px в 360px вьюпорте) ✓ · все 33 картинки грузятся по
  WebP-пути, битых 0 ✓ · HTML-валидация 0 битых ссылок / теги сбалансированы ✓ ·
  вес страницы 1.16 MB при цели <1.5 MB ✓ · live https://fellmise.com отдаёт новую
  версию (9 карточек, `styles.css?v=2`, Podkova), www и http редиректят на HTTPS ✓ ·
  runClient n/a
- **Commit:** 47ea42c, 9802475

**Impact trace**

Изменено (`fellmise_site/`):
- `styles.css` — типографика Podkova/Nunito/PT Mono, sticky-шапка + бургер, блок `.pitch`, `min-width: 0` на элементах грида (фикс H-скролла), мобильные позиции спрайтов, иконки полосы 72→84px, убрана авто-расстановка переносов в слогане
- `main.js` — мобильное меню, поповер Devlog (закрытие по клику вне и Esc)
- `tools/build_site.py` — новые шрифты, sticky-навигация, карточка «Дом», 16 ресурсов, якорь `#world`, кэш-бастер `?v=2`
- `tools/export_web.py` — `ALIASES` (алиас `feat_home` ← `hero_house_b` на 640px)
- `index.html`, `ru/index.html` — перегенерированы
- `assets/` — +7 спрайтов (`res_staff`, `res_spellbook`, `res_runes`, `res_axe`, `res_shield`, `res_dagger`, `feat_home`) в WebP+PNG, обновлён `res_wood`

Создано (`fellmise_site/`):
- `tools/gen_batch2.py` — генерация батча 2 на том же боевом пресете
- `tools/finish_batch2.py` — фильтр/пик/вырез только для новых id (полный `filter_site_pack` откатил бы фиксы дыр и v2 `res_iron`)

Изменено (`fellmise/`):
- `GDD.md` — блок «⚠️ ОТКРЫТО — Жильё игрока (housing)», ссылка из «Средняя игра»; самоописание стиля → hand-painted top-down 2D + запрет тега Pixel Graphics; два UI-упоминания «пиксель-арт» и историческая запись о стиле
- `docs/ENTITIES.md` — «Дом игрока» помечен ⚠️ **открыт** со ссылкой на блок GDD
- `tracker/systems/building.md` — секция «Дизайн-вопросы» с пунктом housing, статус 0/7
- `tracker/ui/localization.md` — «пиксель-шрифтов UI» → «шрифтов UI»
- `tracker/marketing/marketing.md` — закрыты DNS, Enforce HTTPS и итерация 2 (6/8)

---

## 2026-07-26 06:20 — MVP-сайт fellmise.com: вёрстка, web-экспорт, деплой
- **Что:** собран и задеплоен статический двуязычный сайт (EN/RU) из арт-пака v1.
  Hero — CSS-сцена (небо → трава → тропа) со спрайтами на absolute-позициях,
  parallax на скролле, покачивание деревьев, тинт день/ночь/закат/рассвет по
  локальному времени посетителя. 8 карточек-«домиков», полоса ресурсов,
  дисклеймер, футер, SEO/OG/hreflang. Перед вёрсткой закрыты два дефекта пака:
  `res_iron` перегенерён с цветовым якорем (v1 на всех 4 сидах давал серый
  камень, железо не читалось), и у 5 спрайтов выбита прозрачность в замкнутых
  областях фона. Деплой — GitHub Pages, `p1pk4/fellmise-site`, CNAME
  `fellmise.com`; DNS у регистратора переключается руками.
- **Тип:** feat
- **Проверка:** HTML-валидация ✓ (0 битых ссылок, 26/26 img с alt, теги
  сбалансированы) · Pages build ✓ · отдача 200 по EN, RU, css, js, webp, og ✓
  (через `--resolve` на IP Pages, пока DNS не переключён) · вес страницы 0.98 MB
  при цели <1.5 MB ✓ · runClient n/a
- **Commit:** 91175d4, 4d86a0a

**Impact trace**

Создано (`fellmise_site/`):
- `index.html`, `ru/index.html` — генерируются `tools/build_site.py`, коммитятся как статика
- `styles.css`, `main.js`
- `assets/` — 26 WebP + 26 PNG-fallback, `favicon-32.png`, `apple-touch-icon.png`, `icon-512.png`, `og.png`, `og.jpg`
- `tools/build_site.py`, `tools/export_web.py`, `tools/fix_holes.py`, `tools/regen_res_iron.py`
- `CNAME`, `.nojekyll`, `.gitignore`, `DEVLOG.md`

Изменено (`out/site_assets/final/`):
- `res_iron.png` — перегенерация v2 (сид 4004), новый промпт с цветовым якорем
- `feat_skills.png`, `res_bow.png`, `hero_well.png`, `hero_cart.png`, `feat_world.png` — выбиты замкнутые области фона (11 областей)
- `out/site_assets/report.json` — обновлены `chosen`/`object` для `res_iron`, помечен семантический дрейф `feat_death`

Изменено (`fellmise/`):
- `tracker/marketing/marketing.md` — новый файл, статусы домена / арт-пака / деплоя

---

## 2026-07-25 23:33 — Арт-пак сайта v1: 26 объектов на боевом пресете
- **Что:** сгенерирован пак для сайта на пресете `objects_battle_v1`
  (FLUX.1-dev fp8 + `fellmise_objects_v1` @0.7, без blue2d, euler/simple,
  20 шагов, guidance 3.5, cfg 1.0, 1024²): 26 заданий × 4 сида = 104 кадра.
  Автофильтр — флуд-филл из углов (заливка ≥15%, один связный объект, ≤2 края),
  импортируется из `acceptance_metrics`, а не переписан. Годность 104/104,
  спрайт получили 26/26 заданий. Обе экзотики (корабль-призрак, тотем) прошли
  без коллапса к постройке. Персонажи, портреты фракций и UI-иконки не
  генерировались — за границей применимости v1.
- **Тип:** feat
- **Проверка:** флуд-филл 104/104 ✓ · прозрачность финалов 26/26 ✓ ·
  визуальный просмотр контактного листа ✓ (3 кадра перевыбраны по типу:
  `res_pickaxe` — топор вместо кирки, `res_bow` — арбалет, `feat_vendetta` —
  крен к постройке; id спрайта переименован в итерации 3) · runClient n/a
- **Commit:** 91175d4 (внесён вместе с сайтом — репозиторий заведён этим батчем)

**Impact trace**

Создано (`fellmise_site/`):
- `tools/site_tasks.py`, `tools/gen_site_pack.py`, `tools/filter_site_pack.py`
- `out/site_assets/` — 104 кадра, 26 вырезанных PNG с прозрачностью, `report.json`, `contact_sheet.png`
