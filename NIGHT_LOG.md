# ЭТАП 2 — journey v3, наполнение

Блоки Б0→Б5. Всё в `/next/`, прод-корень (заглушка) не трогается, репозиторий
игры не трогается. Скриншоты в `out/night_report/`.

| Время | Блок | Статус | Что сделано / что дальше |
|---|---|---|---|
| 04:05 | **Б0** | **OK** | GPU был свободен (обучения нет: 2.1 из 24 GB — это мой же headless ComfyUI). Сгенерировано **на `fellmise_objects_v1`**: `fellmise_sprite_v2` в пайплайне есть, но приёмки владельца по нему не было, а ТЗ требует при отсутствии приёмки брать v1. Пресет поднят из `objects_battle_v1_archived.json` и переутверждён ассертами. 28 позиций × 4 сида = **112 кадров, 0 ошибок, 99 мин**. Фильтр (тот же импортированный флуд-филл, что и у всего пака): **107/112 годных (96%)**, спрайт есть у всех 28 позиций. Градация под уже отгруженный пак: цель насыщенности снята с 48 существующих спрайтов, не пересчитана заново — новый реквизит обязан войти в существующий вид, а не сдвинуть его |
| 04:40 | **Б1** | **OK** | Свет по форме объекта. `tools/make_emissive.py` вырезает из самих спрайтов маски свечения по правилу на источник (окно тона + порог яркости и насыщенности) → `<id>_em.webp` (сам источник) и `<id>_bleed.webp` (заражение, gauss σ9). 10 источников: жаровня, портал, кристаллы, фонарь, окна двух домов, таверна, свечи склепа, рудная жила, шахта. **Круглые glow-спрайты удалены полностью** — включая заливку у гейтов, она теперь тоже формы своего проёма. Пульсация по типу: огонь — шум (две октавы value-noise, не синус), портал/кристаллы — медленный пульс 0.85 рад/с, окна — статика. В сцене 21 источник, 38 планов, 0 битых текстур, 0 ошибок |

| 05:20 | **Б2** | **OK** | Два типа текста. **Тип A, «в мире»** — слова на предмете, стоящем в сцене: слоган на дощатом щите у деревенской дороги и «Кровная месть» на каменной плите в мире духов. Лицо доски рисуется в canvas (рама, доски/камень, затем текст с автоподбором кегля под длину строки) и идёт текстурой плоскости, поэтому текст живёт в тумане и освещении сцены, а не наклеен поверх. Обе локали. **Тип B, оверлей** — остальные три остановки, как было. В DOM слова типа A остаются: в 3D они понижены до подписи под доской (для скринридеров и поиска), в статическом фолбэке показываются целиком. Один источник копий — `build_site.I18N`/`FEATURES` |

| 06:35 | **Б4** | **OK** | Контраст текста проверен **инструментально**, а не на глаз: `tools/check_contrast.py` + сборщик снимают каждый кадр с невидимым текстом и меряют фон **под** каждым блоком по факту, а не по CSS-подложке (беды полупрозрачные и лежат на 3D-кадре). Считается худший случай в боксе, не средний. Было 4 реальных провала — кнопка Steam 4.19, ссылка смены языка над светлым небом леса 4.11, плюс артефакт замера на прокручиваемой полосе ресурсов. Сейчас **112/112 блоков проходят AA** (4.5:1 обычный текст, 3:1 крупный). Draw calls **до постпроцессинга: 19**, треугольников 26, текстур 61. **Створки дверей открываются в 3D** у деревни и склепа: две створки вырезаны из самого спрайта смещением текстуры (ни одного лишнего байта), петля — в geometry (origin сдвинут на косяк), поворот по Y до ~77°, наружу к камере |

| 07:20 | **Б3** | **OK** | Наполнение разложено **рядами по глубине**, +55 предметов: небо с плывущими облаками и луной в финале, холмы задниками, деревенский двор (амбар, стог, телега, забор, тропа с травой и камнями), лес (грибы, папоротники, валуны, ветка в верх кадра, расфокусированная), шахта (две балки поперёк прохода — камера пролетает под ними, сталагмиты, вагонетка, фонари на цепях, кучи руды), мир духов (два куста надгробий, свечи, валун), финал (сундук, стог, забор). Забор взят существующий `hero_fence` — второй такой же только съел бы бюджет первого кадра. В сцене 234 меша, 93 текстуры, 25 источников света, 6 плывущих облаков |
| 09:40 | **Б5** | **OK** | Смоук на текущем состоянии: 328 проб (насквозь ×2 в обе стороны × 2 локали), **0 кадров с потерянной текстурой**, 0 ошибок, 0 битых ссылок, heap 11 MB. Вес как отдаёт Pages: первый кадр **1.15 MB EN / 1.36 MB RU** (цель 1.5), весь путь 1.66 / 1.95 MB, `next/` 3.50 MB (цель 6). Перемеряно после Б3: смоук чистый, первый кадр **1.10 MB EN / 1.39 MB RU**, весь путь 2.21 / 2.50 MB, heap 13-14 MB. Контраст после наполнения — снова 112/112 AA. Фолбэк живой: 390px, reduced-motion и `?static=1` уходят в статику, горизонтального скролла нет, битых картинок нет, блоки типа A показываются целиком |

**Б0, что промахнулось по типу** (пик по резкости не умеет отличать объект — правки
записаны в `PICK_OVERRIDE` с причиной у каждой):
`haystack` 2002/4004 — пни, взят 1001 · `grave_c` 2002 нёс выдуманную надпись по камню
(классический текстовый артефакт), взят 4004 · `lantern_chain` 4004 — крошечный фонарь со
светящейся подставкой, не читался бы как подвесной, взят 2002 · `rock_s`/`rock_m` — три
валуна одной формы в трёх размерах, разведены по сидам · `cloud_b` 4004 — нитка с двумя
отвалившимися крапинами, взят 3003 · `hill_dark` 3003 — вулканический конус, взят 1001 ·
`stalagmite_a`/`b` — **сталагмитов не вышло ни на одном сиде**, все четыре читаются как
маленькие скалы с норой; взяты самые «шпилевые», в сцене стоят мелко.
**`branch_canopy` — брак по типу на всех 4 сидах** (везде целое дерево, а не свисающая
ветка): позиция сгенерирована, но **в сцену не поставлена**. Свисающей ветки в кадре нет.

**Б3, что пришлось починить:** `beam_frame` оказался сплошным щитом, а не аркой — на высоте
головы камера влетала В него; поднят на высоту балки, проходим снизу. Луна в закатном тумане
превращалась в оранжевый диск — выведена из тумана. Дорога: `tile_road` — это картинка
дороги с травой, запечённой по краям, и при любом повторении трава ложилась поперёк пути;
`tools/make_path_tile.py` вырезает песчаную середину, рендер включает зеркальное повторение,
обочины растворяются в траву вершинной альфой. Холмы на земле читались как зелёные шары —
утоплены за горизонт, видны только гребни.

**Б3, скрины:** `b3_<биом>_{a,b,c}.png` — три точки в каждом биоме, DOM-оверлей скрыт.

**Ещё починено по дороге:** полы биомов стыковались встык, и в паре метров от камеры через
кадр шла жёсткая линия, где почти чёрный камень шахты встречал яркую траву — теперь пол
растворяется на своём последнем отрезке, пока следующий проявляется, и пересечение
приходится на проём. Спрайты-биллборды не касались земли — под предметами планов 1–2
появилось контактное пятно. Окклюдеры у камеры уходят в расфокус смещением mip-уровня в
сэмплере: та же текстура, ни одного лишнего байта.

**Б4, что пришлось починить:** створки с первого раза открывались ВНУТРЬ, в тёмный проём,
и были не видны — знак поворота. Прямоугольники створок сняты по процентной сетке поверх
спрайта, а не на глаз. Заливка гейта была привязана к 0.55 высоты, а несёт след самого
спрайта — оконное свечение уезжало на крышу; теперь центрирована как кадр.

**Соответствие типов:** деревня — слоган, тип A (щит) · лес — карточки, тип B · шахта —
карточки, тип B · мир духов — «Кровная месть», тип A (плита) + «Смерть — это место» тип B ·
дом — финальный блок и полоса ресурсов, тип B.

**Б2, что пришлось починить:** щит 1200×620 давал в мире полосу шириной 11 единиц — шире
дома, и уезжал за край кадра. Стал портретным 900×700 + ножки. Правая полоса деревни и мира
духов расчищена (дом, ящики, «Кровная месть», жаровня сдвинуты), иначе доску загораживали
постройки. Линейка под заголовком перечёркивала первую строку подзаголовка — интервал увеличен.

**Б2, скрины:** `b2_{en,ru}_<биом>.png` (доска в кадре) и `b2_caption_{en,ru}_{village,spirit}.png`
(подпись в DOM при обычном скролле).

**Б1, что пришлось починить:** аддитив на полную силу выбивал крупные источники в
белый — рудный ручей и кристаллы теряли цвет. Сила источника теперь считается из его
же покрытия (`доля светящихся пикселей от непрозрачных`): окно на тёмной стене берёт
0.85, кристаллы и ручей — 0.32. Записывается в `assets/emissive.json` рядом с типом
пульсации, рендер ничего не угадывает.

**Б1, скрины:** `b1_emissive_sheet.png` (12 источников после batch 7, выкл/вкл/с заражением на уровне
ассета) и `b1_light_<биом>_{off,em,all}.png` (то же в сцене) + `b1_gate<N>_peak.png`.

**Б1, известное:** у `biome_crystals` и `feat_mining` в самом ассете запечён светлый
ореол по силуэту — виден и при выключенном свете. Это дефект исходной картинки, не
рендера; на приёмку.

# NIGHT RUN — journey v3 (Three.js) — этап 1

Ночной прогон блоками Б1→Б7. После каждого блока: коммит с тегом, скриншоты в
`out/night_report/`, строка здесь. Правило: если блок не чинится за разумное
число попыток — откат к последнему зелёному, `SKIPPED` с причиной, идём дальше.
К утру `main` и `/next/` обязаны быть живыми.

**Ограничения прогона:** генерация арта и любые обращения к ComfyUI/GPU
запрещены (на машине идёт обучение LoRA, пайплайн переехал в `D:\Dev\ART_Fellmise`
— не тронут). Только существующие спрайты из `assets/`. Прод-корень (заглушка) не
трогается. Репозиторий игры не трогается вообще.

| Время | Блок | Статус | Что сделано / что дальше |
|---|---|---|---|
| 00:11 | старт | — | инвентаризация: 22 уникальных спрайта в `assets/`, тайлов земли нет |
| 00:24 | — | инфо | тайлы `tile_*` взяты из ветки `journey-v2` (собраны PIL из существующих кадров, без GPU) |
| 00:52 | **Б1** | **OK** | Vite+three+gsap+lenis+postprocessing, fullscreen canvas, DPR cap 2, resize, Lenis→ScrollTrigger проксирование, рельса 5607px (~620vh), DOM-оверлей = фолбэк. Обе локали живые, 0 битых картинок, 0 ошибок. Дальше: Б2 диорамы |
| 01:05 | **Б2** | **OK** | 5 диорам по оси Z, 4-6 планов глубины у каждой, окклюдеры у камеры из увеличенных затемнённых спрайтов, грунт TilingSprite-плоскостью, туман со сменой цвета по биому. Починено: плоскости грунта перекрывали соседние биомы и лежали в одной плоскости Y — шахта рисовалась на траве |
| 01:40 | **Б3** | **OK** | CatmullRom через все биомы с боковым уходом, ScrollTrigger scrub → позиция + look-at с опережением, замедление в биомах / ускорение в переходах, mouse-parallax (только desktop). Починено: сегменты скролла делили кривую равными долями, а у гейтов больше контрольных точек — «пик» перехода приходился уже за проёмом |
| 02:10 | **Б4** | **OK** | 4 перехода: дверь дома, вход-скала шахты, портал, дверь склепа. Камера летит В проём, additive-квад света нарастает и заливает кадр, за светом стоит следующая диорама. Створки не анимируются (этап полировки, по ТЗ). Починено: свет стоял ЗА фасадом и был не виден; сегмент гейта теперь центрирован на самом проёме |
| 02:35 | **Б5** | **OK** | Частицы Points: угли+искры над жаровней, светлячки в лесу (16), души (8), пылинки в шахте (26), листья в деревне. UnrealBloom с высоким порогом (0.78) — светятся только огонь/кристаллы/портал/окна. Тонировка деревни по локальному времени. Дыхание деревьев ±0.5° |
| 02:45 | — | инфо | расстояние между биомами 120→150, туман поджат: соседний биом просвечивал над горизонтом бледной полосой |
| 03:10 | **Б7** | **OK** | Смоук: скролл насквозь ×2 в обе стороны на обеих локалях — 204 пробы, **0 кадров с потерянной текстурой**, 0 ошибок, heap 10-11 MB, 41 текстура. Вес по сети (как отдаёт Pages, с gzip у текста): **первый кадр 0.72 MB** (<1.5), **весь `next/` 1.84 MB** (<5). fps 20 — но это **SwiftShader, софтверный WebGL в headless**, к реальной видеокарте отношения не имеет |
| 03:10 | **Б6** | **частично** | Тексты привязаны к сегментам биомов, появляются/уходят по прогрессу (opacity+translate), полоса ресурсов в финале горизонтальным рельсом, подложки за текстом для контраста. **Не сделано:** инструментальная проверка контраста ≥4.5:1 по всем парам — вынесено на утро |
| 03:40 | **деплой** | **OK** | `fellmise.com/next/` и `/next/ru/` — Three.js journey запускается на проде (5 биомов, 4 гейта, 0 битых картинок, горизонтального скролла нет). Мобила 390px уходит в статику. Корень-заглушка не тронут: 4837B, 0 карточек |

## Итог ночи

**Сделано:** Б1 ✅ · Б2 ✅ · Б3 ✅ · Б4 ✅ · Б5 ✅ · Б6 частично · Б7 ✅

**SKIPPED / не доделано:**
- **Б6, контраст** — инструментальная проверка ≥4.5:1 по всем парам текст/фон не написана.
  Подложки за текстом сделаны (кремовые карточки, деревянная доска, тёмная плашка у
  дисклеймера), визуально читается на всех биомах, но числом это не подтверждено.
- **stats.js** не подключён: fps считался покадрово через `requestAnimationFrame` плюс
  `renderer.info`. Причина — цифра всё равно нерепрезентативна в headless (SwiftShader),
  отдельная библиотека ничего бы не добавила.
- **draw calls / triangles** через `renderer.info` не читаются осмысленно: их сбрасывает
  проход постпроцессинга. Нужен отдельный замер, если понадобится.
- **Ленивая загрузка** сделана как «биом 0 до первого кадра, остальные в фоне подряд», а
  не строго «соседний заранее». На 41 текстуре и 1.84 MB это не узкое место.

**Дырки в сценах** закрыты существующими спрайтами (увеличенные затемнённые копии деревьев,
скал и сухостоя как задники и окклюдеры) — генерация не запускалась, `D:\Dev\ART_Fellmise`
не тронут.

**Чего в этом каркасе намеренно нет** (этап полировки, по ТЗ): анимация дверных створок.
